{
  "name": "nullius",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.7.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r --if-present build",
    "lint": "pnpm --filter @nullius/core build && pnpm --filter @nullius/server build && pnpm -r --if-present lint",
    "typecheck": "pnpm --filter @nullius/core build && pnpm --filter @nullius/server build && pnpm -r --if-present typecheck",
    "test": "pnpm -r --if-present test"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
